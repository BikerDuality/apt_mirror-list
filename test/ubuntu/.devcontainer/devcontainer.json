{
    "name":"ubuntu_apt_test",
    "dockerComposeFile": ["../docker-compose.yml"],
    "service":"ubuntu_apt_test",
    "workspaceFolder": "/etc/apt/",
    "shutdownAction": "stopCompose",
    "initializeCommand":[
        "eval",
        "docker image prune -f;",
    ],
}