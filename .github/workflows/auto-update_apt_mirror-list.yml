name: apt mirror list auto update
on: 
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - run: python auto-update_apt_mirror-list.py
      - run: git config --global user.email "22326595+BikerDuality@users.noreply.github.com"
      - run: git config --global user.name "BikerDuality"
      - run: git add -A
      - run: git commit -m "$(date)"
      - run: git push
